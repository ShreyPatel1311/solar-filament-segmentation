# CPU-friendly end-to-end check of the dilation-122436 path.
name: smoke_dilation

data:
  image_size: 128
  val_fraction: 0.5
  num_workers: 0

model:
  architecture: dilation-122436
  in_channels: 1
  base_channels: 8
  encoder_weights: null
  norm: true
  grad_checkpoint: true

train:
  epochs: 1
  batch_size: 2
  accumulation_steps: 2
  amp: false

postprocess:
  threshold: 0.5
  min_area: 5
  closing_radius: 1
