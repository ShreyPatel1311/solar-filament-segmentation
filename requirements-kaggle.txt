# Packages the Kaggle GPU image does not ship, installed by
# notebooks/kaggle_runner.ipynb. Pinned here so a fix is a repo commit rather
# than a notebook edit.
#
# albucore MUST stay pinned next to albumentations: albumentations 1.4.15 puts
# no upper bound on it, and albucore >= 0.0.17 moved the helpers it imports, so
# an unpinned install dies at import with
#     ImportError: cannot import name 'preserve_channel_dim' from 'albucore.utils'
# These pull only pure-Python / CPU dependencies, so a normal (resolved)
# install is safe here; see requirements-kaggle-nodeps.txt for the rest.
albumentations==1.4.15
albucore==0.0.16
pycocotools==2.0.8
munch==4.0.0
